# Partner Core

역할: Open5GS Partner 5GC, OsmoSTP M3UA SG/listener, Partner SEPP, synthetic Diameter server.

Canonical entry point:

```bash
sudo -E ./partner-lab.sh status
sudo -E ./partner-lab.sh ss7
sudo -E ./partner-lab.sh diameter
```

See also: [`docs/CONFIGURATION_REFERENCE.md`](../docs/CONFIGURATION_REFERENCE.md) and [`docs/VALIDATION.md`](../docs/VALIDATION.md).
